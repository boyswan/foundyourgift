version: 0.1
phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - npm run buildprod
artifacts:
  files:
    - package.json
    - buildspec.yml
    - Dockerfile
    - Dockerrun.aws.json
    - public/**/*
